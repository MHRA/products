name: import
version: "0.1"
about: Imports SPC/PIL/PAR PDFs for MIP
author: Stuart Harris <stuart.harris@red-badger.com>

settings:
  - ArgRequiredElseHelp

subcommands:
  - spcpil:
      about: import SPCs and PILs from local directory
      args:
        - directory:
            short: d
            required: true
            takes_value: true
            help: directory to import
  - par:
      about: import PARs from local directory
      args:
        - directory:
            short: d
            required: true
            takes_value: true
            help: directory to import
