name: $(Date:yyyyMMdd)$(Rev:.r)

                  # $(containerRegistry)/$(imageRepository):$(tag)

stages:
  - stage: Build
    displayName: Build stage
    jobs:  
    - job: Build
      displayName: Build job
      pool:
        vmImage: ubuntu-latest
      steps:
      - task: Docker@2
        displayName: Build and push an image to container registry
        inputs:
          command: buildAndPush
          repository: products/api
          dockerfile: $(Build.SourcesDirectory)/medicines/api/Dockerfile
          containerRegistry: mhraproductsdevregistry
          tags: |
            api-pipeline
####################
  # - stage: Deploy
  #   displayName: Deploy stage
  #   dependsOn: Build
  #   jobs:
  #   - deployment: Deploy
  #     displayName: Deploy job
  #     pool:
  #       vmImage: ubuntu-latest
  #     environment: dev
  #     strategy:
  #       runOnce:
  #         deploy:
  #           steps:
  #           - task: KubernetesManifest@0
  #             displayName: Deploy to Kubernetes cluster
  #             inputs:
  #               action: deploy
  #               namespace: default
  #               manifests: |
  #                  $(Build.SourcesDirectory)/medicines/api/infrastructure/development/deployment.yml
  #                  $(Build.SourcesDirectory)/medicines/api/infrastructure/development/service.yml
  #                  $(Build.SourcesDirectory)/medicines/api/infrastructure/development/virtual-service.yml
  #               containers: |
  #                 mhraproductsdevregistry/products/api:api-pipeline